# Fails for bad input

    Code
      keep_first("bob")
    Condition
      Error in `keep_first()`:
      ! `x` must be an incidence2 object.

---

    Code
      keep_last("bob")
    Condition
      Error in `keep_last()`:
      ! `x` must be an incidence2 object.

